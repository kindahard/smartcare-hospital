kind = "web"
previewPath = "/"
title = "SmartCare HMS"
version = "1.0.0"
id = "artifacts/smartcare"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25876

[services.development]
run = "pnpm --filter @workspace/smartcare run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/smartcare", "run", "build" ]
publicDir = "artifacts/smartcare/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25876"
BASE_PATH = "/"
